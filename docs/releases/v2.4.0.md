Tốt. Bắt đầu phase **v2.4.0 Release Cleanup / Stabilization**.

Mục tiêu phase này là khóa lại toàn bộ các phần đã làm:

```text
v2.3.4 GEDCOM Import/Merge
v2.3.5 Data Maintenance
v2.3.6 Admin Health
v2.3.7 Vietnamese Tree Hardening
```

Không thêm feature lớn nữa. Chỉ làm cleanup, checklist, changelog, route audit, release tag.

---

## 1. Tạo branch release cleanup

```bash
cd /opt/giapha-os

git checkout main
git pull origin main

git checkout -b release-v2.4.0-cleanup
git push -u origin release-v2.4.0-cleanup
```

Chạy baseline:

```bash
bun run test
bun run build
```

---

# Bước 1 nên làm: Release Changelog

Tạo file:

```bash
mkdir -p docs/releases

nano docs/releases/v2.4.0.md
```

Dán nội dung này:

````md
# Release v2.4.0

## Tổng quan

v2.4.0 là bản ổn định sau các phase lớn:

- GEDCOM Import/Merge v2.3.4
- Data Maintenance v2.3.5
- Admin Health v2.3.6
- Vietnamese Tree Hardening v2.3.7

Mục tiêu của bản này là khóa lại hệ thống genealogy sau khi chuyển sang Family Model + Event Model, bổ sung GEDCOM staging an toàn, công cụ bảo trì dữ liệu, health dashboard và diagnostics cho cây gia phả tiếng Việt.

---

## 1. GEDCOM Import/Merge

### Hoàn thành

- GEDCOM export dùng stable UUID XREF.
- GEDCOM parser giữ UUID XREF khi import lại.
- Staging import trước khi ghi DB chính.
- Duplicate detection cho person.
- Match Review cho person trùng/nghi trùng.
- Round-trip validation report.
- Merge Plan preview-only.
- Merge Suggestions review layer.
- Commit approved merge suggestions bằng RPC.
- Post-import audit report.
- Import Safety Gate.
- GEDCOM Import/Merge runbook.

### Kết quả kỳ vọng

File GEDCOM export từ chính app khi import lại phải đạt:

```text
persons = matches
possibleMatches = 0
errors = 0
person create pending = 0
````

Không commit session round-trip.

---

## 2. Data Maintenance

### Hoàn thành

* Data Maintenance index page.
* Unknown Persons Manager.
* Duplicate Events Preview.
* Soft-delete duplicate birth/death events.
* Events Missing Links Preview.
* Repair missing person_events links.
* Empty Families Preview.
* Soft-delete empty families.
* Data Maintenance runbook.

### Các RPC chính

```text
repair_events_missing_person_links()
soft_delete_empty_families()
soft_delete_duplicate_birth_death_events()
```

### Kết quả sạch kỳ vọng

```text
Unknown persons = 0
Events missing links = 0
Duplicate event groups = 0
Empty families = 0
```

---

## 3. Admin Health

### Hoàn thành

* Admin Health overview page.
* Link Admin Health vào dashboard/header/Data Quality.
* Metrics:

  * Unknown persons
  * Events missing links
  * Duplicate event groups
  * Empty families
  * Open import sessions
  * Pending merge suggestions
  * Approved merge suggestions
* Admin Health runbook.

### Ý nghĩa

Admin Health là trang kiểm tra nhanh sau migration/import/maintenance.

Route:

```text
/dashboard/admin-health
```

---

## 4. Vietnamese Tree Hardening

### Hoàn thành

* Tree Diagnostics Panel.
* Layout performance measurement.
* Large tree warning.
* Diagnostics snapshot copy.
* Clipboard fallback cho HTTP LAN.
* Auto-collapse guard cho cây lớn.
* Layout invariant tests.
* Vietnamese Tree hardening runbook.

### Diagnostics gồm

```text
Total persons
Families
Visible nodes
Visible people
Family groups
Expanded
Collapsed
Max depth
Spouse nodes
Multi-spouse
Width
Height
Layout ms
Measured
```

---

## 5. Route checklist

Các route chính cần hoạt động:

```text
/dashboard
/dashboard/members
/dashboard/stats
/dashboard/data-quality
/dashboard/data-maintenance
/dashboard/data-maintenance/unknown-persons
/dashboard/data-maintenance/duplicate-events
/dashboard/data-maintenance/events-missing-links
/dashboard/data-maintenance/empty-families
/dashboard/admin-health
/dashboard/import
/dashboard/import/<SESSION_ID>
/dashboard/import/<SESSION_ID>/matches
/dashboard/import/<SESSION_ID>/merge
/dashboard/import/<SESSION_ID>/audit
/dashboard/vietnamese-tree-test
```

---

## 6. Final SQL audit

Chạy trước khi tag release:

```sql
SELECT COUNT(*) AS active_unknown_left
FROM public.persons
WHERE full_name IN ('Unknown', 'Chưa rõ tên')
  AND deleted_at IS NULL;

SELECT COUNT(*) AS events_without_person_events
FROM public.events e
WHERE e.deleted_at IS NULL
  AND e.legacy_person_id IS NOT NULL
  AND e.type IN ('birth', 'death')
  AND NOT EXISTS (
    SELECT 1 FROM public.person_events pe
    WHERE pe.event_id = e.id
      AND pe.person_id = e.legacy_person_id
  );

WITH grouped AS (
  SELECT
    e.legacy_person_id,
    e.type,
    e.start_date,
    e.sort_date,
    COUNT(*) AS c
  FROM public.events e
  WHERE e.deleted_at IS NULL
    AND e.legacy_person_id IS NOT NULL
    AND e.type IN ('birth', 'death')
  GROUP BY e.legacy_person_id, e.type, e.start_date, e.sort_date
  HAVING COUNT(*) > 1
)
SELECT COUNT(*) AS duplicate_groups
FROM grouped;

SELECT COUNT(*) AS active_empty_families
FROM public.families f
WHERE f.deleted_at IS NULL
  AND NOT EXISTS (
    SELECT 1 FROM public.family_parents fp
    WHERE fp.family_id = f.id
  )
  AND NOT EXISTS (
    SELECT 1 FROM public.family_children fc
    WHERE fc.family_id = f.id
  );

SELECT COUNT(*) AS approved_merge_suggestions
FROM public.import_merge_suggestions
WHERE status = 'approved';

SELECT COUNT(*) AS pending_merge_suggestions
FROM public.import_merge_suggestions
WHERE status = 'pending';
```

Kỳ vọng tốt:

```text
active_unknown_left = 0
events_without_person_events = 0
duplicate_groups = 0
active_empty_families = 0
approved_merge_suggestions = 0 hoặc đã hiểu rõ
pending_merge_suggestions = 0 hoặc đang review
```

---

## 7. Test/build

Trước khi merge:

```bash
bun run test
bun run build
```

---

## 8. Release tag

Sau khi merge vào main:

```bash
git tag -a v2.4.0 -m "v2.4.0 GEDCOM Import, Data Maintenance, Admin Health, Vietnamese Tree hardening"
git push origin v2.4.0
```

---

## 9. Ghi chú không làm trong v2.4.0

Không thêm feature lớn:

* Không đổi schema mới nếu không cần.
* Không đổi layout algorithm lớn.
* Không sửa GEDCOM logic lớn.
* Không thêm import mode mới.
* Không refactor toàn app.

v2.4.0 chỉ là release cleanup/stabilization.

````

---

## 2. Chạy test/build

```bash
bun run test
bun run build
````

---

## 3. Commit changelog

```bash
git status

git add docs/releases/v2.4.0.md

git commit -m "docs(release): add v2.4.0 changelog"

git push
```

---

## 4. Bước tiếp theo sau changelog

Sau khi commit changelog, ta làm tiếp **Route Audit Script** để tự kiểm tra những file route quan trọng tồn tại trước khi release.
